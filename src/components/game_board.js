import React from 'react';
import Square from './square';

const GameBoard = () => {
  const rows = 3;
  const columns = 3;

  return (
    <div 
      onClick={() => markSquare
    className="square"></div>
  );
};

export default GameBoard;
